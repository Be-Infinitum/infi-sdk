DATABASE_URL="file:./dev.db"

# From your claimed sandbox
INFI_SECRET_KEY=sk_test_replace_me
INFI_API_URL=http://localhost:8088

# Merchant slug used to build the hosted checkout URL (/pay/{slug}/invoices/{id})
INFI_SLUG=ebook-demo
# Where the hosted checkout page lives (frontend app). Local: http://localhost:3000
INFI_PAY_BASE_URL=http://localhost:3000

# Webhook signing secret (from POST /account/webhooks)
INFI_WEBHOOK_SECRET=whsec_replace_me

# The ebook file/link delivered after payment (used by the seed)
EBOOK_DOWNLOAD_URL=https://example.com/the-lean-side-project.pdf
EBOOK_PRICE_BRL=49.00
