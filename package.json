{
  "name": "infi-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/sdk",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '@infi/sdk' build",
    "test": "bun run --filter '@infi/sdk' test",
    "typecheck": "bun run --filter '@infi/*' typecheck",
    "codegen": "bash scripts/codegen.sh"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
