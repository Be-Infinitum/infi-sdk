{
  "name": "infi-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/sdk",
    "packages/nextjs",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '@beinfi/*' build",
    "test": "bun run --filter '@beinfi/*' test",
    "typecheck": "bun run --filter '@beinfi/*' typecheck",
    "codegen": "bash scripts/codegen.sh"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
